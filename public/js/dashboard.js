// Add Job button
    // Fetch POST {title, link, company_name, description, salary_information, contact_information, application_status, additional_comments}

    // Refresh screen to load new job

// Delete Job button
    // Fetch DELETE

    // Refresh screen to load without job

// Job button
    // Redirect user to job.html

// Calendar button
    // Redirect user to calendar.html
